package name.rayrobdod.fightStage;

/**
 * A placeholder to represent something that can affect the choice of animation,
 * but which the core classes themselves don't particularly care about
 */
public interface AttackModifier {
}
